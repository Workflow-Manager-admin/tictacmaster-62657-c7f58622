# Set the database URL for SQLAlchemy (e.g., for PostgreSQL)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/tic_tac_toe

# By default, uses local SQLite file (no need to set this variable for local development)
